@extends('layouts.app')
@section('title',$isEdit?'Editar clínica':'Adicionar clínica')
@section('content')
<div class="mb-4"><h1 class="page-title">{{ $isEdit?'Editar clínica':'Adicionar clínica' }}</h1><p class="page-description mb-0">Informe os dados da clínica.</p></div><form method="POST" action="{{ $isEdit?route('clinicas.update',$clinica->id):route('clinicas.store') }}">@csrf @if($isEdit)@method('PUT')@endif<div class="card content-card"><div class="card-header"><h5>Dados da clínica</h5></div><div class="card-body p-4">@if($errors->any())<div class="alert alert-danger"><strong>Verifique os campos.</strong><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="row g-3"><div class="col-md-8"><label for="titulo" class="form-label">Título</label><input id="titulo" name="titulo" maxlength="50" class="form-control" value="{{ old('titulo',$clinica->titulo) }}" required></div><div class="col-md-4"><label for="consultorios" class="form-label">Quantidade de consultórios</label><input id="consultorios" name="consultorios" type="number" min="0" class="form-control" value="{{ old('consultorios',$clinica->consultorios) }}" required></div><div class="col-12"><input type="hidden" name="ativo" value="0"><div class="form-check form-switch"><input id="ativo" name="ativo" value="1" type="checkbox" class="form-check-input" @checked((bool)old('ativo',$clinica->ativo))><label for="ativo" class="form-check-label">Status ativado</label></div></div></div><div class="d-flex justify-content-end gap-2 mt-4"><a href="{{ route('clinicas.index') }}" class="btn btn-outline-secondary">Cancelar</a><button class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Salvar</button></div></div></div></form>
@endsection
