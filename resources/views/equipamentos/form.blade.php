@extends('layouts.app')
@section('title',$isEdit?'Editar equipamento':'Adicionar equipamento')
@section('content')
<div class="mb-4"><h1 class="page-title">Cadastro de Equipamentos do Checklist</h1><p class="page-description mb-0">{{ $isEdit?'Atualize os dados do equipamento.':'Cadastre um equipamento para uso nos checklists.' }}</p></div><form method="POST" action="{{ $isEdit?route('equipamentos.update',$equipamento->id):route('equipamentos.store') }}">@csrf @if($isEdit)@method('PUT')@endif<div class="card content-card"><div class="card-header"><h5>Dados do equipamento</h5></div><div class="card-body p-4">@if($errors->any())<div class="alert alert-danger"><strong>Verifique os campos.</strong><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="row g-3"><div class="col-12"><label for="titulo" class="form-label">Título</label><input id="titulo" name="titulo" maxlength="100" class="form-control" value="{{ old('titulo',$equipamento->titulo) }}" required></div><div class="col-12"><input type="hidden" name="ativo" value="0"><div class="form-check form-switch"><input id="ativo" name="ativo" value="1" type="checkbox" class="form-check-input" @checked((bool)old('ativo',$equipamento->ativo))><label for="ativo" class="form-check-label">Status ativado</label></div></div></div><div class="d-flex justify-content-end gap-2 mt-4"><a href="{{ route('equipamentos.index') }}" class="btn btn-outline-secondary">Cancelar</a><button class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Salvar</button></div></div></div></form>
@endsection
