@extends('layouts.app')
@section('title',$isEdit?'Editar manutenção corretiva':'Adicionar manutenção corretiva')
@push('styles')<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet">@endpush
@section('content')
<div class="mb-4"><h1 class="page-title">Lançamento de Manutenções Corretivas</h1><p class="page-description mb-0">{{ $isEdit?'Atualize os dados do lançamento.':'Cadastre uma manutenção corretiva.' }}</p></div>
<form method="POST" action="{{ $isEdit?route('corretiva.update',$corretiva->id):route('corretiva.store') }}">@csrf @if($isEdit)@method('PUT')@endif<div class="card content-card"><div class="card-header"><h5>Dados da manutenção corretiva</h5></div><div class="card-body p-4">@if($errors->any())<div class="alert alert-danger"><strong>Verifique os campos.</strong><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="row g-3"><div class="col-12"><label for="ativos_id" class="form-label">Ativo / Equipamento</label><select id="ativos_id" name="ativos_id" class="form-select" required><option value="">Selecione um ativo</option>@foreach($ativos as $ativo)@php($local=($ativo->local?->titulo?:'Local não informado').' - '.($ativo->local?->unidade?->titulo?:'Unidade não informada'))<option value="{{ $ativo->id }}" @selected((string)old('ativos_id',$corretiva->ativos_id)===(string)$ativo->id)>{{ $ativo->titulo }} ({{ $ativo->codigo?:'sem código' }}) - {{ $local }}</option>@endforeach</select></div><div class="col-md-6"><label for="solicitante" class="form-label">Solicitante</label><input id="solicitante" name="solicitante" maxlength="50" class="form-control" value="{{ old('solicitante',$corretiva->solicitante) }}" required></div><div class="col-md-6"><label for="data_agendamento" class="form-label">Data da manutenção</label><input id="data_agendamento" name="data_agendamento" type="date" class="form-control" value="{{ old('data_agendamento',$corretiva->data_agendamento?->format('Y-m-d')?:today()->format('Y-m-d')) }}" required></div><div class="col-12"><label for="observacao" class="form-label">Status da manutenção</label><textarea id="observacao" name="observacao" rows="3" maxlength="512" class="form-control">{{ old('observacao',$corretiva->observacao) }}</textarea></div><div class="col-12"><input type="hidden" name="ativo" value="0"><div class="form-check form-switch"><input id="ativo" name="ativo" value="1" type="checkbox" class="form-check-input" @checked((bool)old('ativo',$corretiva->ativo))><label for="ativo" class="form-check-label">Status ativado</label></div></div></div><div class="d-flex justify-content-end gap-2 mt-4"><a href="{{ route('corretiva.index') }}" class="btn btn-outline-secondary">Cancelar</a><button class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Salvar</button></div></div></div></form>
@endsection
@push('scripts')<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script><script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script><script>$(function(){$('#ativos_id').select2({theme:'bootstrap-5',width:'100%',placeholder:'Selecione ou pesquise um ativo',language:{noResults:()=> 'Nenhum ativo encontrado'}})});</script>@endpush
