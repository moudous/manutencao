@extends('layouts.app')
@section('title','Visualizar manutenção corretiva')
@section('content')
@php $ativo=$corretiva->equipamento;$local=($ativo?->local?->titulo?:'—').' - '.($ativo?->local?->unidade?->titulo?:'Unidade não informada');$campos=[['ID',$corretiva->id],['Ativo / Equipamento',($ativo?->titulo?:'—').' ('.($ativo?->codigo?:'sem código').')'],['Local - Unidade',$local],['Solicitante',$corretiva->solicitante],['Data da manutenção',$corretiva->data_agendamento?->format('d/m/Y')],['Status da manutenção',$corretiva->observacao],['Etapa',$corretiva->etapa],['Ativação',$corretiva->ativo?'Ativada':'Desativada'],['Criado em',$corretiva->data_lancamento?->format('d/m/Y H:i')]]; @endphp
<div class="d-flex justify-content-between align-items-start mb-4"><div><h1 class="page-title">Lançamento de Manutenções Corretivas</h1><p class="page-description mb-0">Visualização do lançamento corretivo.</p></div><a href="{{ route('corretiva.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Voltar</a></div><div class="card content-card"><div class="card-header"><h5>Dados da manutenção corretiva</h5></div><div class="card-body p-4"><div class="row g-3">@foreach($campos as [$label,$value])<div class="col-md-6"><div class="form-label">{{ $label }}</div><div class="form-control bg-body-tertiary h-auto text-break">{{ filled($value)?$value:'—' }}</div></div>@endforeach</div></div></div>
@endsection
