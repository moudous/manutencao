@extends('layouts.app')
@section('title','Visualizar lançamento')
@section('content')
<div class="mb-4"><h1 class="page-title">Visualizar lançamento</h1><p class="page-description mb-0">Detalhes do lançamento da agenda preventiva.</p></div>
<div class="card content-card"><div class="card-header"><h5>Dados do lançamento</h5></div><div class="card-body p-4">@php $equip=$lancamento->equipamento; $campos=[['ID',$lancamento->id],['Agenda',$lancamento->agenda_id],['Ativo / Equipamento',$equip?($equip->titulo.' ('.($equip->codigo?:'sem código').')'):'—'],['Local - Unidade',($equip?->local?->titulo?:$lancamento->local?->titulo?:'—').' - '.($equip?->local?->unidade?->titulo?:'Unidade não informada')],['Solicitante',$lancamento->solicitante],['Data do lançamento',$lancamento->data_lancamento?->format('d/m/Y H:i')],['Problema',$lancamento->problema],['Observação',$lancamento->observacao],['Data do orçamento',$lancamento->data_orcamento?->format('d/m/Y H:i')],['Data do agendamento',$lancamento->data_agendamento?->format('d/m/Y H:i')],['Técnico',$lancamento->tecnico?->nome?:$lancamento->tecnicos_id],['Situação (ID)',$lancamento->situacao_id],['Tipo (ID)',$lancamento->tipos_id],['Etapa',$lancamento->etapa],['Data de arquivamento',$lancamento->data_arquivamento?->format('d/m/Y H:i')],['Status',$lancamento->ativo?'Ativo':'Inativo'],['Criado em',$lancamento->criado_em?->format('d/m/Y H:i')],['Atualizado em',$lancamento->atualizado_em?->format('d/m/Y H:i')]]; @endphp<div class="row g-3">@foreach($campos as [$label,$value])<div class="col-md-6"><div class="form-label">{{ $label }}</div><div class="form-control bg-body-tertiary h-auto text-break">{{ filled($value)?$value:'—' }}</div></div>@endforeach</div><div class="d-flex justify-content-end mt-4"><a href="{{ route('agenda.lancamentos.index',$agenda) }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Voltar</a></div></div></div>
@endsection
