<div class="d-inline-flex gap-1">@if($giPermissoes->permite('compras.visualizar'))<a href="{{ route('compras.show',$compra->id) }}" class="btn btn-sm btn-outline-dark listagem-acao" title="Visualizar"><i class="bi bi-eye-fill"></i></a>@endif @if(!$compra->trashed()) @if($giPermissoes->permite('compras.criar'))<a href="{{ route('compras.edit',$compra->id) }}" class="btn btn-sm btn-outline-dark listagem-acao" title="Editar"><i class="bi bi-pencil-square"></i></a>@endif @if($giPermissoes->permite('compras.excluir'))<form method="POST" action="{{ route('compras.destroy',$compra->id) }}" class="excluir-compra-form">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger listagem-acao"><i class="bi bi-trash-fill"></i></button></form>@endif @else @if($giPermissoes->permite('compras.restaurar'))<form method="POST" action="{{ route('compras.restore',$compra->id) }}" class="restaurar-compra-form">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success listagem-acao"><i class="bi bi-arrow-counterclockwise"></i></button></form>@endif @if($giPermissoes->permite('compras.excluir_permanentemente'))<form method="POST" action="{{ route('compras.force-destroy',$compra->id) }}" class="apagar-compra-form">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-dark listagem-acao"><i class="bi bi-trash-fill"></i></button></form>@endif @endif</div>
